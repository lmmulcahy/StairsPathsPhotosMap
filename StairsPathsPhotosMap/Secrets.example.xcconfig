// Copy this file to Secrets.xcconfig and fill in your own values.
// Secrets.xcconfig is gitignored so your real key is never committed.
//
// The Google Maps key is only required if you use the Google Maps backend
// (Settings > Map Preference). Apple Maps works without it.

GOOGLE_MAPS_API_KEY =
